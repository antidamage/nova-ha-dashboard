"use client";

// Facade: the dashboard's zone, entity and desktop commands. The body lives in
// ./state/useDashboardCommands.ts; hold timings are in ./state/constants.ts.
export { useDashboardCommands } from "./state/useDashboardCommands";
export type { ApplyEntityActionsOptions } from "./state/types";
