"""Implementation package for the Tuya mobile -> MQTT bridge.

The entry point stays at `bridge.py` beside this package; the container runs
`python3 /app/bridge.py`. Import submodules directly -- this package
deliberately re-exports nothing.
"""
